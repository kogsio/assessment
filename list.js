
function join(list) {
    console.log(list.join());
    return (list.join());
}

// // --- do not change code below this line ---
// try{ module.exports=join; }
// catch(e){ console.log('testing section') };
